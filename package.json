{
  "name": "agilize-institucional-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'PHP projects do not use npm dev'",
    "build": "echo 'PHP projects do not use npm build'",
    "preview": "echo 'PHP projects do not use npm preview'"
  }
}
